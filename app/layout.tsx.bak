import type { Metadata } from "next";
import { Inter } from "next/font/google";
import Link from "next/link";
import {
  Activity,
  ShieldCheck,
  Building2,
  FolderKanban,
  PlusCircle,
  FileCheck,
} from "lucide-react";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const metadata: Metadata = {
  title: "Macula Healthcare | Clinical Intelligence & Governance",
  description:
    "Enterprise clinical transcription, master synthesis, and DPDP/NMC compliance supervision platform.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="h-full bg-slate-50 antialiased">
      <body className={`${inter.variable} min-h-full font-sans text-slate-900 flex flex-col`}>
        {/* Top Regulatory Compliance & Supervision Banner */}
        <div className="bg-slate-900 px-4 py-1.5 text-center text-[11px] font-medium text-slate-300 border-b border-slate-800 flex items-center justify-center gap-2">
          <ShieldCheck className="h-3.5 w-3.5 text-teal-400" />
          <span>
            Operating under <strong>NMC Registered Medical Practitioner (RMP) Supervision</strong> &amp; <strong>DPDP Act Data Protection</strong>
          </span>
        </div>

        {/* Global Navigation Header */}
        <header className="sticky top-0 z-40 border-b border-slate-200 bg-white/95 backdrop-blur-xs">
          <div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-3">
            {/* Platform Brand */}
            <div className="flex items-center gap-6">
              <Link href="/" className="flex items-center gap-2">
                <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-teal-600 text-white shadow-xs">
                  <Activity className="h-5 w-5" />
                </div>
                <div>
                  <span className="text-base font-bold tracking-tight text-slate-900">
                    Macula
                  </span>
                  <span className="ml-1 text-xs font-semibold text-teal-600">
                    Clinical
                  </span>
                </div>
              </Link>

              {/* Navigation Links */}
              <nav className="hidden md:flex items-center gap-1 text-xs font-semibold text-slate-600">
                <Link
                  href="/cases/new"
                  className="flex items-center gap-1.5 rounded-lg px-3 py-1.5 hover:bg-slate-100 hover:text-slate-900 transition"
                >
                  <PlusCircle className="h-3.5 w-3.5 text-teal-600" />
                  <span>New Case Ingestion</span>
                </Link>

                <Link
                  href="/admin/cases"
                  className="flex items-center gap-1.5 rounded-lg px-3 py-1.5 hover:bg-slate-100 hover:text-slate-900 transition"
                >
                  <FolderKanban className="h-3.5 w-3.5 text-slate-500" />
                  <span>Case Management</span>
                </Link>

                <Link
                  href="/admin/organizations"
                  className="flex items-center gap-1.5 rounded-lg px-3 py-1.5 hover:bg-slate-100 hover:text-slate-900 transition"
                >
                  <Building2 className="h-3.5 w-3.5 text-slate-500" />
                  <span>Hospital Networks</span>
                </Link>
              </nav>
            </div>

            {/* Quick Ingestion Call-to-Action */}
            <div className="flex items-center gap-3">
              <Link
                href="/cases/new"
                className="flex items-center gap-1.5 rounded-xl bg-teal-600 px-3.5 py-1.5 text-xs font-semibold text-white shadow-xs hover:bg-teal-700 transition"
              >
                <PlusCircle className="h-3.5 w-3.5" />
                <span>Dictate Case</span>
              </Link>
            </div>
          </div>
        </header>

        {/* Primary Page Outlet */}
        <div className="flex-1">
          {children}
        </div>

        {/* Global Regulatory Footer */}
        <footer className="border-t border-slate-200 bg-white py-4 text-center text-xs text-slate-400">
          <div className="mx-auto max-w-7xl px-6 flex flex-col sm:flex-row items-center justify-between gap-2">
            <div className="flex items-center gap-1.5">
              <FileCheck className="h-3.5 w-3.5 text-teal-600" />
              <span>Macula Clinical Intelligence Platform &copy; {new Date().getFullYear()}</span>
            </div>
            <div className="flex items-center gap-4 text-[11px]">
              <span>NMC Code of Medical Ethics (2023)</span>
              <span>•</span>
              <span>DPDP Act 2023 PHI Compliance</span>
              <span>•</span>
              <span>HIPAA BAA Storage</span>
            </div>
          </div>
        </footer>
      </body>
    </html>
  );
}
