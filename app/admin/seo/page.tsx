"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowLeft, CheckCircle2, Loader2, Save } from "lucide-react";

export default function SeoPage() {
  const [organizationId, setOrganizationId] = useState("");
  const [sets, setSets] = useState<any[]>([]);
  const [loading, setLoading] = useState(true);
  const [message, setMessage] = useState<string | null>(null);
  useEffect(() => { (async () => { const orgs = await (await fetch("/api/admin/organizations")).json(); const org = orgs.organizations?.[0]; if (!org) return setLoading(false); setOrganizationId(org.id); const data = await (await fetch(`/api/admin/seo?orgId=${org.id}`)).json(); setSets(data.sets || []); setLoading(false); })().catch(() => setLoading(false)); }, []);
  const update = async (set: any) => { const response = await fetch("/api/admin/seo", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id: set.id, primaryKeyword: set.primaryKeyword, secondaryKeywords: set.secondaryKeywords, longTailKeywords: set.longTailKeywords, localKeywords: set.localKeywords, questionKeywords: set.questionKeywords, semanticKeywords: set.semanticKeywords, searchIntent: set.searchIntent }) }); const data = await response.json(); setMessage(response.ok ? "SEO record saved." : data.error || "Unable to save SEO record."); if (response.ok) setSets((current) => current.map((item) => item.id === set.id ? data.set : item)); };
  return <main className="readable-route ml-0 mr-auto max-w-5xl space-y-6 p-6 md:p-8"><header><div className="mb-1 flex items-center gap-2 text-xs font-semibold uppercase tracking-wider text-pine"><Link href="/" className="flex items-center gap-1 hover:underline"><ArrowLeft className="h-3 w-3" /> Dashboard</Link><span>/</span><span>SEO</span></div><h1 className="text-2xl font-bold tracking-tight text-ink">SEO Quality</h1><p className="mt-1 text-xs text-muted">Review keywords, search intent, duplicate hashes, and article quality before publication.</p></header>{message && <p className="rounded border border-pine/30 bg-pine-tint p-3 text-xs text-pine">{message}</p>}{loading ? <div className="p-12 text-center text-sm text-muted"><Loader2 className="mx-auto mb-2 h-5 w-5 animate-spin" />Loading SEO records…</div> : sets.length === 0 ? <div className="rounded border border-dashed border-line bg-surface p-12 text-center text-sm text-muted">No SEO records yet.</div> : <div className="space-y-4">{sets.map((set) => <section key={set.id} className="rounded border border-line bg-surface p-5"><div className="flex items-center justify-between"><div><h2 className="text-sm font-bold text-ink">{set.case.title}</h2><p className="text-xs text-muted">Quality score: {set.qualityScore ?? "Not assessed"} · {set.case.status}</p></div>{set.qualityScore >= 70 && <CheckCircle2 className="h-5 w-5 text-sage" />}</div><div className="mt-4 grid gap-3 md:grid-cols-2"><label className="text-xs text-muted">Primary keyword<input value={set.primaryKeyword || ""} onChange={(event) => setSets((current) => current.map((item) => item.id === set.id ? { ...item, primaryKeyword: event.target.value } : item))} className="mt-1 w-full rounded border border-line bg-paper p-2 text-xs text-ink" /></label><label className="text-xs text-muted">Search intent<input value={set.searchIntent || ""} onChange={(event) => setSets((current) => current.map((item) => item.id === set.id ? { ...item, searchIntent: event.target.value } : item))} className="mt-1 w-full rounded border border-line bg-paper p-2 text-xs text-ink" /></label></div><label className="mt-3 block text-xs text-muted">Secondary keywords, comma separated<textarea value={Array.isArray(set.secondaryKeywords) ? set.secondaryKeywords.join(", ") : ""} onChange={(event) => setSets((current) => current.map((item) => item.id === set.id ? { ...item, secondaryKeywords: event.target.value.split(",").map((value) => value.trim()).filter(Boolean) } : item))} className="mt-1 h-16 w-full rounded border border-line bg-paper p-2 text-xs text-ink" /></label><label className="mt-3 block text-xs text-muted">Long-tail keywords, comma separated<textarea value={Array.isArray(set.longTailKeywords) ? set.longTailKeywords.join(", ") : ""} onChange={(event) => setSets((current) => current.map((item) => item.id === set.id ? { ...item, longTailKeywords: event.target.value.split(",").map((value) => value.trim()).filter(Boolean) } : item))} className="mt-1 h-16 w-full rounded border border-line bg-paper p-2 text-xs text-ink" /></label><button onClick={() => update(set)} className="mt-3 flex items-center gap-1 rounded bg-pine px-3 py-1.5 text-xs font-semibold text-white"><Save className="h-3 w-3" />Save and reassess</button>{Array.isArray(set.validationIssues) && set.validationIssues.length > 0 && <ul className="mt-3 list-disc pl-5 text-xs text-ochre">{set.validationIssues.map((issue: string) => <li key={issue}>{issue}</li>)}</ul>}</section>)}</div>}</main>;
}